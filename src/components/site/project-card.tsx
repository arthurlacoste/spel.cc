import { withBasePath } from "@/lib/base-path"
import Image from "next/image"
import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { RiArrowRightUpLine } from "@remixicon/react"
export function ProjectCard({logo,title,kind,description,href}:{logo:string,title:string,kind:string,description:string,href:string}) { return <a href={href} className="group"><Card className="h-full p-6 transition-transform group-hover:-translate-y-1"><CardHeader className="p-0"><div className="flex items-start justify-between"><Image src={logo} alt={`${title} logo`} width={58} height={58} className="size-14 rounded-xl object-contain"/><span className="text-xs uppercase tracking-widest text-muted-foreground">{kind}</span></div><CardTitle className="mt-8 text-2xl">{title}</CardTitle><CardDescription className="mt-2 min-h-20 text-sm leading-6">{description}</CardDescription><span className="mt-8 inline-flex items-center gap-1 text-sm font-medium">Discover <RiArrowRightUpLine className="size-4"/></span></CardHeader></Card></a> }
